#!/usr/bin/node
// Script that prints 3 lines using an array and loop

const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const language of languages) {
  console.log(language);
}
